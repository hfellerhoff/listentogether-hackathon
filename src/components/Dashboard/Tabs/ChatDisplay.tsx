import React from 'react';
import { Box } from '@chakra-ui/core';

interface Props {}

const ChatDisplay = (props: Props) => {
  return <Box flex={1} background='transparent'></Box>;
};

export default ChatDisplay;
